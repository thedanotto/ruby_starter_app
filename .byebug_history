c
quit
retrieve_hand_values(hand).uniq
!is_two_pair?(hand)
is_pair?(hand)
c
more_than_1.length
more_than_1 = counts.reject { |val| val == 1 }
counts = vih.uniq.map do |val| vih.count(val) end
vih.uniq.map do |val| vih.count(val) end
vih.uniq.each do |val| vih.count(val) end
vih.uniq.each do |val| vih.count(val)
vih.reduce
vih.one?
vih = values_in_hand
[].methods
values_in_hand.sort do |a, b| a <=> b end
values_in_hand
quit
uit
number_values
sorted_values
c
sorted_values
